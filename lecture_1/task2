#Написать функцию подсчета частоты вхождения элементов в интервалы (создать массив на 10 элементов, содержащий гистограмму распределения на 10 интервалов).  Интервалы: 0—99,  100—199, … ,900—999)
import random
def calchist(massiv):
    a=[0]*10
    for elem in massiv:
        a [int(elem//100)]+=1
    return a
print(calchist([random.randrange (1000) for i in range (100)]))
